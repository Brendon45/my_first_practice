#include <stdio.h>

/**
* main - Entry Point
* Question: What is the output of the following code
* Return: 0 (Success)
*/

int main(void)
{
	int a = 5, b= 3;
	int result = a * b + 1;

	printf("%d\n", ++result);
	return (0);
}
